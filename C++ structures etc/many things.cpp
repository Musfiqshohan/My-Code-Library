1.Operator override:
matrix operator * (matrix n)

2.int to string:
std::string s = std::to_string(42);


3. How you multiply two numbers which will overflow but can mod?
- think them as binary numbers then left shift one and add to sum if another has set bit 
